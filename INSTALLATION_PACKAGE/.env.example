# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/mbeki_healthcare

# Application Configuration  
NODE_ENV=production
PORT=5000
SESSION_SECRET=your_secure_random_session_secret_here_change_this

# Email Integration (Optional - for error reporting)
# If using Outlook integration for error reporting to info@champsafrica.com
REPLIT_CONNECTORS_HOSTNAME=
REPL_IDENTITY=
WEB_REPL_RENEWAL=

# Security
ALLOWED_ORIGINS=https://yourdomain.com

# Database Pool Settings (Optional)
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10